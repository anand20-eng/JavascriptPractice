1) what is call, appply and  Bind ?

Call: The call() method invokes a function with a given this value and 
argumnets provided one by one

Apply: The apply invokes a function with given this value and arguments provided   as a array type

bind
var employee2 = {firstName: 'john', lastName: 'Rodson'};
var employee2 = { firstName: "Jimmy", lastName: "Baily" };


function invite(greeting1, greting2) {
   
   console.log( greeting+, " " + this.firstName + " " + this.lastName + greeting2);
  
}

invite.call(empployee1, "Hello", 'how are you')

invite.apply(employee1, ['Hello', 'how are you'])
 
var invieEmppoyee1 = invite.bind(empleoyee1);
var invieEmppoyee1 = invite.bind(empleoyee2);
invieEmppoyee1(''hello','how are you');
invieEmppoyee2(''hello','how are you')

what is josn ?

jSON means javascript object Notation

Used to store and exchange data;


JSON.parse(text);


JSON.stringigy(object);







4) what is higher order fucntion 

Higher-oreder is function that accepts anohter function as an argument

or return a function as a return value or both

function Welcome(name) {
  var greetingInfo = function (message) {
	console.log(message + "" + name);	
	
  }'
  return greeetingInfo;
  
   var MyFunction = Welcome('john');
   
   
}




///////// what is promise 

syntax of Promise:-

const promise = new Promise(function(resovle, reject) {

// promise description
});







