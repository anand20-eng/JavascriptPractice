A pseudo-class is used to define a special state of an element

--  Styel an element when a user mouse over it 
--   style an visted and unvisted link differnely 
----  Style an element when it gets focus
// unvisted link
a:link {
  color: red;
}

// visited link 
a:visted {
  color: green
}
// mouse over link 
a:hover {
 color: hotpink;
}
// selected  link 
a: active {
  color: blue
}
///////////////

a.highlight:hover {
   color: #ff0000;

}

//// tooltip 

p {
  dipaly: none;
  background-color: yellow;
  padding: 20px;
}

div:hover p {
 dispaly: block
}

p:first-child {
  color: blue;
}
p i:first-child {
    color:blue;
}

we can divide CSS selector into five categories

1) simple selectors (select elements based  on name, id, class); s s
2) combinator selectors (select elements based on a  specific rleationship between them) c s
3) Pseudo-class selectors (select elements based on a certaine state); ps c s
4) Pseduo-element selecotors(select elements based on a certain state)
5) Attributes selector (select elements based on an attribute or attribute value)

